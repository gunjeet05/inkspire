specVersion: "v1beta"
serviceId: "inkspire"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "inkspire-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
